# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env
.env*.local
.env.development
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo

# idea files
.idea

# personal Claude Code settings (not shared)
/.claude/settings.local.json

# production build output, kept separate so it cannot clobber a running dev server
/.next-build/

# smoke-test screenshots, rewritten on every run
/.smoke/
build-results.png

# a second dev server needs its own cache, or two of them clobber each other
/.next-dev*/
